# UI check: phase-3

**Date:** 2026-06-14
**Result:** PASS
**Dev server:** http://localhost:3100

## E2E (Playwright)

| Check | Result | Notes |
|-------|--------|-------|
| npm run test:e2e | PASS | 16 passed, 4 skipped, 0 failed (23.4s); desktop + mobile projects |

**Passed tests (16):**

| Project | Test |
|---------|------|
| desktop | initial load shows pokemon grid |
| desktop | empty detail state on desktop |
| desktop | select Pikachu shows detail |
| desktop | selected card highlight |
| desktop | rapid switching keeps detail in sync |
| desktop | no uncaught console errors on happy path |
| desktop | search filters list |
| desktop | scroll loads more pokemon |
| desktop | grid row sprites do not overlap card above |
| mobile | initial load shows pokemon grid |
| mobile | select Pikachu shows detail |
| mobile | selected card highlight |
| mobile | no uncaught console errors on happy path |
| mobile | search filters list |
| mobile | scroll loads more pokemon |
| mobile | detail opens as modal with close |

**Skipped (4):** empty detail (mobile), rapid switching (mobile), grid overlap (mobile), modal close (desktop).

## Browser scenarios

| # | Scenario | Result | Notes |
|---|----------|--------|-------|
| 1 | Initial load — card names visible | PASS | E2e waits for `.loading-screen` hidden before grid visible |
| 2 | Select cached Pokemon — no double-jump | NOT RUN | UX/feel scenario — manual only |
| 3 | Scroll list (desktop ≥1100px) — fixed detail | NOT RUN | Fixed panel is CSS-only; no scroll+position e2e |
| 4 | Select different Pokemon — smooth transition | NOT RUN | Animation/feel; no e2e assertion |
| 5 | Long detail content — internal scroll | NOT RUN | No e2e for `.detail-card` overflow scroll |
| 6 | Stats section — horizontal layout | PASS | E2e Pikachu detail: 7 `.detail-stats .stat-row` elements |
| 7 | Detail loading — rotating Pokeball | NOT RUN | `.detail-loading-ball` not exercised in e2e |
| 8 | Back-to-top after one viewport | NOT RUN | `BackToTop` visibility/click not in e2e |
| 9 | Rapid switching (cached) — no stale detail | PASS | E2e rapid-switch desktop: Charizard after #1 → #6 |
| 10 | Route error boundary | NOT RUN | `error.tsx` not triggerable in current e2e suite |
| 11 | Page metadata (title, OG) | NOT RUN | No title/meta e2e |

**Supplemental:**

| Check | Result | Notes |
|-------|--------|-------|
| Grid row sprites do not overlap card above | PASS | Desktop e2e; `.pokemon-grid` row gap `64px` (was `24px`) |

## Issues found

### Critical
- (none)

### Major
- (none) — user-reported sprite overlap resolved; e2e confirms second-row sprite clears first-row card bottom

### Minor
- Scenarios 2–5, 7–8, 10–11 lack Playwright coverage; optional manual pass
- Larger grid row gap (`64px`) is a deliberate spacing tradeoff to prevent sprite overlap

## Console / dev log

| Level | Message | When |
|-------|---------|------|
| — | No uncaught `pageerror` events | E2e happy path on desktop and mobile |

## Recommended fixes

1. None required for Phase 3 UI sign-off.
2. Optional: add e2e for back-to-top, detail loading Pokeball, and metadata.
